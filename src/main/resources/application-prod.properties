# Prod settings: real SMTP provider values should be provided via environment or secure vault
bank.config.daily-withdrawal-limit=5000
bank.config.support-email=support@bank.com
bank.config.region=Americas
bank.config.api-timeout=5000

spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
